export const developmentConfig = {
  // Set this to true to disable rate limiting in development
  disableRateLimiting: false,
  
  // Set this to true to allow all origins in development
  allowAllOrigins: false,
  
  // Other development-specific settings
  logging: {
    level: 'debug',
    requests: true
  }
}; 